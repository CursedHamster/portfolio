import styles from "./loading.module.scss";

const Loading = () => {
  return (
    <div className={styles.loading_container}>
      {/* <div
        className={`${styles.bracket} ${styles.left} loading_bracket_left`}
      ></div>
      <div className={`${styles.logo} loading_logo`}></div>
      <div
        className={`${styles.bracket} ${styles.right} loading_bracket_right`}
      ></div> */}
    </div>
  );
};

export default Loading;
